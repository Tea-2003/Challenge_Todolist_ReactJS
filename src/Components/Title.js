import React, { Component } from 'react';

class Title extends Component {
    render() {
        return (
            <div className="todo">
                #todo
            </div>
        );
    }
}

export default Title;